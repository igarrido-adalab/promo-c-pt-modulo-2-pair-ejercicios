const title=document.querySelector('.js_title');

const nombre=document.querySelector('.js_nombre'); 

const adalaber=title.innerHTML + ' ' + '<span>' + nombre.innerHTML + '</span>';

title.innerHTML=adalaber;